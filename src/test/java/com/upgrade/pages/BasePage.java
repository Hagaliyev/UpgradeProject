package com.upgrade.pages;

public abstract class BasePage {

    /**
     * This abstract page class is for design purposes. I am not writing anything in this class because I have only one test scenario to test
     * In real framework with a lot of pages we can store common elements and actions in this page which will be extended by page classes and extend this abstract class and inherit all properties and actions from this class and this way we can avoid duplicate code in different page classes
     */
}
